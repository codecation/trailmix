en:
  date:
    formats:
      default:
        "%m/%d/%Y"
      day_of_week:
        "%A"
      month_day_year:
        "%B %-d, %Y"
      with_weekday:
        "%a %m/%d/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"
      for_prompt:
        "%A, %b %-d" # Thursday, Sept 17
